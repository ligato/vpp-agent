# Enable or disable feature to measure netlink API call duration. Measured time is shown directly in log (info level).
# Measurement is taken also for certain procedures, like resync of plugin startup. Turned off by default.
stopwatch: true

# Used to disable entire linux plugin functionality. Turned off by default.
disabled: false