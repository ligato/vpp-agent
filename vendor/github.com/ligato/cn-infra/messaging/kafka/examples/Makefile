
build:
	cd asyncproducer && go build
	cd consumer && go build
	cd syncproducer && go build
	cd mux && go build

clean:
	rm -f asyncproducer/asyncproducer
	rm -f consumer/consumer
	rm -f syncproducer/syncproducer
	rm -f mux/mux