# buf.yaml - Buf config file
#   docs: https://buf.build/docs/configuration

build:
  roots:
    - proto

lint:
  use:
    - BASIC
    - FILE_LOWER_SNAKE_CASE
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - FILE
    - WIRE_JSON
