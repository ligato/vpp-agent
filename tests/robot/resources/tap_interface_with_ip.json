{
  "name": "${name}",
  "type": 3,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "ip_addresses": [
    "${ip}/${prefix}"
  ],
  "tap": {
    "host_if_name": "${host_if_name}"
  }
}
