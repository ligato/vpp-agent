{
  "name": "${name}",
  "type": 3,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "vrf": ${vrf},
  "ip_addresses": [
    "${ip}/${prefix}",
    "${second_ip}/${second_prefix}"
  ],
  "tap": {
    "version": 2,
    "host_if_name": "${host_if_name}"
  }
}
