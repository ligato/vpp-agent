{
  "name": "${name}",
  "type": 2,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": 1500,
  "memif": {
    "master": ${master},
    "id": ${id},
    "socket_filename": "${socket}"
  }
}
