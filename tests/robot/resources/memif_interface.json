{
  "name": "${name}",
  "type": 3,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "vrf": ${vrf},
  "memif": {
    "master": ${master},
    "id": ${id},
    "socket_filename": "${socket}"
  }
}
