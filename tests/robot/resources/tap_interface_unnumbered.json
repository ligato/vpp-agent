{
  "name": "${name}",
  "type": 4,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "unnumbered": {
    "is_unnumbered": ${unnumbered},
    "interface_with_ip": "${interface_with_ip_name}"
  },
  "tap": {
    "host_if_name": "${host_if_name}"
  }
}
